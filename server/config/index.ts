import IsValidUrl from "../utils/isValidUrl";

export default {
  default: {},
  validator(config) {
    // if (!IsValidUrl(config.deployUrl)) {
    //   throw new Error(`The 'deployUrl' parameter must be a valid url`)
    // }

    // if (!IsValidUrl(config.badgeUrl)) {
    //   throw new Error(`The 'badgeUrl' parameter must be a valid url`)
    // }
  },
};
